// ============================================================
// Morph / 另我 — 配置文件
// 修改此文件即可控制 API 连接、模型参数、功能开关
// ============================================================

const CONFIG = {
  // ----------------------------------------------------------
  // DeepSeek API 配置（必须填写 API_KEY 才能使用）
  // ----------------------------------------------------------
  API: {
    BASE_URL: 'https://api.deepseek.com/v1/chat/completions',
    MODEL: 'deepseek-v4-flash',
    API_KEY: '',                          // 在此填入你的 DeepSeek API Key
    TEMPERATURE: 0.85,
    MAX_TOKENS: 380,
    TOP_P: 0.92,
  },

  // ----------------------------------------------------------
  // 对话历史限制
  // ----------------------------------------------------------
  HISTORY: {
    MAX_ROUNDS: 12,                       // 发送给 API 的最大对话轮数
    MAX_PERSIST: 500,                     // IndexedDB 保留的最大消息数
  },

  // ----------------------------------------------------------
  // UI 设置
  // ----------------------------------------------------------
  UI: {
    WELCOME_DELAY_MS: 800,                // 欢迎气泡延迟（毫秒）
    PLACEHOLDER_TEXT: 'Echo 正在输入...',  // 等待回复时的占位文字
  },
};
